% ============================================================
%  Speaker Script — visual styling (xelatex)
% ============================================================

% --- packages ---
\PassOptionsToPackage{table}{xcolor}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{titlesec}
\usepackage{titling}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{tcolorbox}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{microtype}
\usepackage{ragged2e}
\usepackage{etoolbox}
\tcbuselibrary{skins,breakable}

% --- color palette ---
\definecolor{accent}    {HTML}{1F4E8B} % deep blue
\definecolor{accentDark}{HTML}{122E54}
\definecolor{accent2}   {HTML}{C0531C} % warm orange
\definecolor{soft}      {HTML}{F4F6FA}
\definecolor{rule}      {HTML}{D0D5DD}
\definecolor{ink}       {HTML}{1A1F2C}
\definecolor{muted}     {HTML}{55607A}

% --- typography defaults ---
\setstretch{1.18}
\setlength{\parskip}{0.55em}
\setlength{\parindent}{0pt}
\color{ink}
\RaggedRight

% --- slide heading: large, colored, with a thin underline ---
\titleformat{\section}[hang]
  {\Large\bfseries\color{accentDark}}
  {}{0pt}{}
  [\vspace{2pt}{\color{rule}\titlerule[0.6pt]}]
\titlespacing*{\section}{0pt}{1.6em plus 0.3em}{0.55em}

% --- subsection (used for "Quick reference", "Likely questions", etc.) ---
\titleformat{\subsection}[hang]
  {\large\bfseries\color{accent}}
  {}{0pt}{}
\titlespacing*{\subsection}{0pt}{1.3em}{0.45em}

\titleformat{\subsubsection}[hang]
  {\normalsize\bfseries\color{accentDark}}
  {}{0pt}{}
\titlespacing*{\subsubsection}{0pt}{0.9em}{0.3em}

% --- speaker chip: solid colored pill with name + duration ---
\newcommand{\speakerline}[2]{%
  \par\nobreak\vspace{-0.15em}%
  \noindent
  {\setlength{\fboxsep}{4pt}%
    \colorbox{accent}{%
      \color{white}\strut
      \textbf{\,#1\,}%
      \hspace{6pt}\textcolor{white!75!accent}{\rule[0.4ex]{0.4pt}{0.9ex}}\hspace{6pt}%
      \textit{#2}%
      \hspace{2pt}%
    }%
  }%
  \par\vspace{0.55em}%
}

% --- header / footer ---
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhead[L]{\small\color{muted}\textit{Speaker Script\,\,\textperiodcentered\,\,GAPMAP $\times$ Behavioral Economics}}
\fancyhead[R]{\small\color{muted}\thepage}
\fancypagestyle{plain}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
  \fancyhead[R]{\small\color{muted}\thepage}%
}

% --- table polish ---
\setlength{\arrayrulewidth}{0.4pt}
\arrayrulecolor{rule}
\renewcommand{\arraystretch}{1.28}

% --- list polish ---
\setlist[itemize]{leftmargin=1.2em,itemsep=0.15em,parsep=0.15em,topsep=0.25em}
\setlist[enumerate]{leftmargin=1.4em,itemsep=0.15em,parsep=0.15em,topsep=0.25em}

% --- horizontal rule (markdown ---) softer ---
\providecommand{\pandocbounded}[1]{#1}

% --- title block tightening ---
\setlength{\droptitle}{-2.5em}
\pretitle{\begin{flushleft}\Huge\bfseries\color{accentDark}}
\posttitle{\par\vspace{0.2em}{\color{rule}\rule{\linewidth}{0.6pt}}\end{flushleft}\vspace{-0.4em}}
\preauthor{\begin{flushleft}\large\color{muted}}
\postauthor{\end{flushleft}}
\predate{\begin{flushleft}\small\color{muted}\itshape}
\postdate{\end{flushleft}}

% --- protect against pandoc passing a tightlist command ---
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
